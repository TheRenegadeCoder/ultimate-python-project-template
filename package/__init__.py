from .module import *

def example_package_function(num: int) -> int:
    """
    Squares the input number and returns the result.

    :param int num: a number
    :return: the square of the input
    """
    return num ** 2
